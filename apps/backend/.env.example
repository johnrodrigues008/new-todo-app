# ===========================
# Configurações do Banco de Dados
# ===========================

# Usuário do banco de dados PostgreSQL
POSTGRES_USER=root

# Senha do usuário do banco de dados
POSTGRES_PASSWORD=root

# Nome do banco de dados a ser utilizado
POSTGRES_DB=postgres

# URL de conexão com o banco de dados LOCAL via Docker
# Descomente esta linha se estiver usando Docker localmente
# DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}?schema=public

# URL de conexão com banco externo (exemplo Neon Serverless)
# Descomente e configure conforme seu provedor externo
# DATABASE_URL="postgresql://usuario:senha@endereco_do_servidor/nome_do_banco?sslmode=require&channel_binding=require"

# ===========================
# Configurações da Aplicação
# ===========================

# Chave secreta para geração de tokens JWT — altere para uma string segura e única
SECRETKEY=seu_segredo_seguro_aqui

# Porta onde o backend será exposto localmente
PORT=4000

# Porta autorizada para CORS — normalmente a porta onde o frontend roda
PORTORIGINCORS=3000

# Tempo de expiração do token JWT (ex: 1d, 12h, 30m)
EXPIRESIN=1d

# ===========================
# Variáveis Expostas ao Frontend
# ===========================

# URL da API backend para consumo pelo frontend Next.js
NEXT_PUBLIC_API_URL=http://localhost:4000
